/* jornada-progress-inline.js: DESATIVADO O REPOSICIONAMENTO */
(function(){
  'use strict';
  // O código de realocação foi removido, pois o posicionamento é feito por CSS.
  // Deixamos um stub (código vazio) para evitar erros se outros scripts o chamarem.
  console.log("jornada-progress-inline.js: Realocação desativada. Posicionamento feito via CSS.");

  // Se você precisa de acesso aos IDs para atualizar o progresso,
  // eles continuam sendo:
  // - #progress-block-fill
  // - #progress-question-fill
  // - #progress-total-value
})();
